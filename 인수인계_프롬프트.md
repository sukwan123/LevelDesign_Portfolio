# 이어서 작업하기 — 붙여넣을 프롬프트

아래 전체를 새 대화 첫 메시지로 붙여넣고, `images/` 폴더와 작업할 파일을 함께 첨부한다.

---

나는 손석완, 넷마블네오 ProjectT(왕좌의 게임 IP 오픈월드 액션 어드벤처)의 레벨 디자이너다.
2022-08 입사. 이직용 포트폴리오를 다듬는 중이고,
네가 이어서 작업해야 한다.

## 지금까지 나온 것

**최종 산출물: `포트폴리오_손석완_레벨디자이너_v14.pptx` (44장)**

젠스파크로 39장을 뽑은 뒤 내가 로컬에서 5장을 보태고 이미지를 전부 교체한 것이다.
슬라이드 구성은 표지 / 요약 / 숫자 / 기능·기믹 카드 / 업무비중 인포그래픽 /
지역·던전 9개 섹션(각 평면도·목업 전경·실작업) / 이동 기믹 / AI 파이프라인 / 연표 / 스킬.

담당 콘텐츠는 필드 5곳(최북단·올드타운·하이가든·크로우즈네스트·트윈스)과
던전 5종(그리핀·크라켄·장벽너머 일부·맘모스·하렌홀 PvEvP).

## 파일 규칙 — 이게 전부의 기준이다

`images/` 118장. 파일명이 곧 슬라이드 순서다.

```
섹션번호-슬롯번호_지역_종류_인덱스.jpg
슬롯 1 = 평면도, 2 = 목업 전경, 3 = 실작업(완성 화면)
예) 01-2_최북단_목업_03.jpg
01 최북단 / 02 올드타운 / 03 하이가든 / 04 크로우즈네스트
05 그리핀던전 / 06 크라켄던전 / 07 장벽너머 / 08 맘모스던전 / 09 하렌홀
10-1 = 업무 비중 인포그래픽
```

## 반드시 지킬 것

- **해상도를 훼손하지 마라.** 원본 그대로. 재인코딩도 피한다
- **여러 장을 한 장으로 미리 합치지 마라.** 아래 "젠스파크" 항목 참고
- **실명 그대로** 쓴다. 가명 처리 안 한다
- **미공개 콘텐츠**(트윈스 전체, 맘모스 실작업, 하렌홀 실작업, 넥)는 이미지를 넣지 않고
  "미공개"라고만 쓴다. "준비 중" 같은 표현은 쓰지 않는다
- **사족을 달지 마라.** 이 문서를 보는 사람은 레벨 디자이너 직무 범위를 안다.
  "배경·아트는 유관 부서 담당" 같은 설명은 빼라
- **문장은 함축적으로.** 개별 기능을 나열하지 말고 "레벨 관련 각종 기능 담당"처럼 묶어라
- 외부 저작물(드라마 캡처, 타 게임 스크린샷, 스톡 사진, IP 공식 원화)은 절대 넣지 마라.
  이미 한 번 걸러냈다

## 젠스파크를 다시 쓸 경우 — 실측된 제약

**젠스파크는 업로드 이미지를 긴 변 1024px로 깎는다.** (PDF 39장 안의 이미지 78개 중 75개가 정확히 1024)

- 원본 해상도로 올릴 이유가 없다. 긴 변 1024 Lanczos로 미리 줄여 올려라 (81MB → 23MB)
- **낱장으로 줘라.** 9장을 합쳐 주면 1024px 예산을 9등분해 셀당 340px이 된다.
  낱장으로 주면 각 장이 1024px을 받고, 젠스파크가 여백 없이 꽉 찬 3열 격자를 알아서 만든다
- 첫 첨부만 80개 제한, 추가 첨부는 무제한
- 재요청할 때는 배치 규칙을 새로 가르치지 말고 **이미 잘 나온 슬라이드를 지목**해라
  ("이 덱의 03 실작업 슬라이드에서 쓴 방식 그대로")
- 받은 PPTX는 반드시 `도구/fix_pptx.py`로 원본 해상도를 되돌려라

## 도구 (`도구/` 폴더)

| 스크립트 | 하는 일 |
|---|---|
| `fix_pptx.py <pptx>` | 젠스파크 PPTX의 1024px 축소본을 `images/` 원본으로 교체. 합성본은 낱장 격자로 재배치, 슬라이드 분할과 쪽번호 재기입까지. **가장 중요** |
| `imgmatch.py` | 위가 쓰는 썸네일 서명 매칭 (32×32 그레이, 종횡비 3%, 픽셀당 차이 12 이하) |
| `make_infographic2.py` | 업무 비중 인포그래픽(가로형 2600×860) 생성 |
| `classify2.py` | Perforce 서브밋 1,637건을 카테고리로 분류 (인포그래픽 수치의 근거) |
| `build_upload3.py` | 젠스파크 업로드 세트 생성 (긴 변 1024로 리샘플, 1차/2차 폴더 분할) |
| `build_replace.py` | 교체가 필요한 슬라이드분만 뽑는 최소 첨부 세트 |
| `build_spec.py` | HTML에서 슬라이드 스펙 Markdown 자동 생성 |
| `build_v13.py` | HTML 버전 갱신 |

검증은 PowerPoint COM으로 렌더해서 눈으로 본다.
`$pres.Slides($n).Export($out,"PNG",1600,900)`

## 환경

- **`python` 명령이 깨져 있다. `py -3`을 써라.** node 없음
- 필요한 패키지: Pillow, python-pptx, pymupdf (`py -3 -m pip install ...`)
- 산출물은 **바탕화면에 저장**한다. 수정할 때 덮어쓰지 말고 **vN을 올려 새 파일로** 만든다
- 볼트: `Documents/Obsidian for Seokwan/Obsidian for Seokwan/`
  업무이력 정리물은 `70_업무이력/업무이력 허브.md`가 진입점 (이 폴더 사본이 `업무이력/`에 있다)

## 업무이력 정리를 이어갈 경우

소스별 식별 키:
- **Jira** — `assignee WAS 손석완` 으로 과거 담당까지. 사무 IT(HELPDESK) 이슈는 제외.
  총 597건 관여, 그중 발주(보고자) 100건, 담당했다가 배분 365건
- **Perforce** — 공용 계정 `design`을 쓰므로 워크스페이스명과 `[손석완]` 설명 접두사로 구분.
  `P4CHARSET=utf8` 필요. 1,637건
- **Google Drive** — 공유 드라이브 파일은 `owner='me'`로 안 잡힌다. PSD mimeType은 `image/x-photoshop`
- 2025-02~2025-11 공백은 개인 사유다. 이력서·포트폴리오·산출물 어디에도 기술하지 마라
- 개인 법적·징계 관련 문서(2024-08~09)는 열지도 말고 집계에도 넣지 마라

## 지금 할 일

(여기에 이번에 시킬 작업을 쓴다)

---

## 첨부할 것

- `images/` 118장 — 거의 모든 작업의 입력
- `도구/` 8개 스크립트
- 고칠 대상 파일 (`*.pptx` 또는 `*.html`)
- 필요하면 `업무이력/` (1MB, 텍스트)
